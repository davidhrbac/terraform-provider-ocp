# {{ .Name }}

Looks up a network by name within a customer.

## Example Usage

```hcl
data "ocp_customer" "example" {
  name = "customer-a"
}

data "ocp_network" "example" {
  customer_id = data.ocp_customer.example.id
  name        = "net-a"
}
```
{{ .SchemaMarkdown | trimspace }}
