# {{ .Name }}

Looks up a domain by name within a customer.

## Example Usage

```hcl
data "ocp_customer" "example" {
  name = "customer-a"
}

data "ocp_domain" "example" {
  customer_id = data.ocp_customer.example.id
  name        = "example.com"
}
```
{{ .SchemaMarkdown | trimspace }}
