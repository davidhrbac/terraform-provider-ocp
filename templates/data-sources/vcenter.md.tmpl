# {{ .Name }}

Looks up a vCenter by name within a customer.

## Example Usage

```hcl
data "ocp_customer" "example" {
  name = "customer-a"
}

data "ocp_vcenter" "example" {
  customer_id = data.ocp_customer.example.id
  name        = "vcenter-01"
}
```
{{ .SchemaMarkdown | trimspace }}
