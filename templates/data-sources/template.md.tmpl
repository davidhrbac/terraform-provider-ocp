# {{ .Name }}

Looks up a template by name.

## Example Usage

```hcl
data "ocp_customer" "example" {
  name = "customer-a"
}

data "ocp_template" "example" {
  name        = "ubuntu-22-04"
  customer_id = data.ocp_customer.example.id
  region      = "FINLAND"
}
```
{{ .SchemaMarkdown | trimspace }}
